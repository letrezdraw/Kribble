# Development environment variables
# Use empty strings to leverage Vite's proxy (configured in vite.config.ts)
# This routes API calls through the Vite dev server, avoiding CORS issues
VITE_API_URL=
VITE_SOCKET_URL=
